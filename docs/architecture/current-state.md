# Current API Architecture State

## Current Structure Summary

The repository currently contains a minimal placeholder structure:

- `README.md` identifies the project as CarbonFactor API.
- `LICENSE` contains the project license.
- No API source project, solution file, tests, application configuration, routing layer, domain model, or persistence implementation is present yet.

Based on the available files, the project currently behaves as an API placeholder rather than an API prototype, layered API, clean architecture implementation, or library plus API wrapper.

## Key API Responsibilities

No implemented API responsibilities are present yet. The intended responsibility inferred from the repository name is to expose carbon factor API capabilities, but there are no endpoints or contracts to confirm runtime behavior.

## Current Validation Flow

There is no implemented request, domain, or persistence validation flow.

## Current Normalization Flow

There is no implemented normalization flow for units, categories, sources, regions, dates, or emission values.

## Current Persistence and Query Flow

There is no implemented persistence layer, in-memory store, repository abstraction, or query endpoint.

## Current Test Coverage Observations

There are no test projects or test files in the repository.

## Risks and Limitations

- The repository does not yet provide a runnable API.
- API contracts, validation rules, error responses, and storage behavior are undefined.
- There is no automated test coverage.
- There is no local development configuration or startup documentation beyond the placeholder README.
- Future implementation should establish a small, reviewable foundation before adding larger infrastructure concerns.
