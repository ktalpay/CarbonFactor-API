# CarbonFactor API — Codex Cloud Run 05 — 2026-04-30

## Purpose

Consolidate CarbonFactor API's current technical work into conservative, reviewer-readable evidence documentation.

This run should not add new API behavior. It should document the implemented API contract, transport boundary, FastAPI adapter, tests, limitations, and next steps in a way that supports future evidence preparation without overstating maturity.

## Current Repository Context

The `develop` branch already contains:

- Python package baseline
- API contract dataclasses
- deterministic error helpers
- in-memory synthetic factor catalog
- transport envelopes, status mapping, serialization, and handlers
- thin FastAPI adapter with `/health`, `/factors`, and `/factors/{factor_id}`
- explicit HTTP query validation
- OpenAPI inspection tests
- HTTP error consistency tests
- local-only documentation
- test-only CI

Related ecosystem context:

- CarbonFactor Parser exists separately as deterministic parser/data tooling.
- CarbonOps Assistant exists separately as deterministic assistant/evaluation tooling.
- Mention those only as planned/adjacent integration points. Do not claim integration is implemented here.

## Constraints

- Do not add new API behavior.
- Do not add new routes.
- Do not add new dependencies.
- Do not add storage/database/auth/deployment/cloud/Docker behavior.
- Do not import code from other repositories.
- Keep claims conservative: pre-alpha, local development, synthetic data only.
- Do not claim production usage, adoption, certification, compliance, or official provider compatibility.

## Branch Rule

The Codex Cloud checkout may appear as `work`. Treat the UI-selected `develop` environment as the source of truth.

Rules:

1. Execute only this file.
2. Execute only CFA-018 through CFA-020.
3. Use Codex Cloud PR workflow.
4. Final PR target must be `develop`.
5. Do not modify `main`.

Suggested branch:

```text
feature/codex-cfa-005-evidence-consolidation
```

Suggested PR title:

```text
CFA Cloud Run 05 — Add API evidence consolidation docs
```

## Validation

After each task:

```bash
python -m pytest -q
git status --short
```

Before final PR:

```bash
python -m pytest -q
git status --short
git log --oneline -10
```

Use small commits where supported.

Suggested commits:

```text
CFA-018 Add technical evidence index
CFA-019 Add API architecture evidence summary
CFA-020 Align README and evidence roadmap
```

---

# Task Queue

## CFA-018 — Technical Evidence Index

### Objective

Add a conservative index of implemented technical evidence in this repository.

### Required Changes

Create:

```text
docs/evidence/index.md
```

Update if useful:

```text
README.md
```

### Content Requirements

Summarize implemented work under headings such as:

- API contract foundation
- error model
- in-memory catalog service
- transport boundary
- FastAPI adapter
- HTTP query validation
- OpenAPI inspection tests
- HTTP error consistency tests
- CI and test coverage
- limitations and non-goals

For each evidence item include:

- implemented artifact/file references
- why it matters technically
- evidence relevance, stated conservatively
- current limitation
- next improvement candidate

Do not claim external impact, adoption, production readiness, or official provider compatibility.

---

## CFA-019 — API Architecture Evidence Summary

### Objective

Document how the API layers currently work end-to-end.

### Required Changes

Create:

```text
docs/evidence/api-architecture-evidence.md
```

### Content Requirements

Explain the current local flow:

```text
HTTP request
  -> FastAPI adapter
  -> HTTP query validation
  -> transport handler
  -> catalog service
  -> DTO/serialization
  -> response envelope
```

Include:

- current modules and responsibilities
- contract boundaries
- what is tested
- what is intentionally not implemented
- why this is still pre-alpha
- future extension points

---

## CFA-020 — README and Roadmap Alignment

### Objective

Update repository navigation so a reviewer can quickly find API evidence and architecture documentation.

### Required Changes

Update:

```text
README.md
docs/roadmap.md
docs/evidence-backlog.md
docs/api-contract.md
docs/http-adapter.md
```

### Documentation Requirements

- Link to `docs/evidence/index.md` from README.
- Link to `docs/evidence/api-architecture-evidence.md` from README or architecture docs.
- Update roadmap/backlog to reflect evidence consolidation as implemented.
- Keep future items conservative.
- Do not add production, compliance, certification, official-provider, or adoption claims.

---

## Final Report

At the end, report:

```text
A. Summary
B. Files changed
C. Tests run and result
D. Safety scan result
E. Commits created with hashes
F. Pull request result, including PR target branch
G. Remaining risks
H. Recommended next task
```
